spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shopflow_db
    username: postgres
    password: Asmatp1234.
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    com.shopflow: DEBUG
    org.springframework.security: DEBUG